<h2>Artist: <%= @artist.name%></h2>
<h2> <%=@artist.song_count.to_s + " song"%> </h2>
<h2>Songs: </h2>
<ul>
<% @artist.songs.each do |song|%>
    <li><%= link_to song.title, song_path(song)%></li>
<% end %>
</ul>
